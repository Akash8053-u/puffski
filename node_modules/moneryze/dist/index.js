"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __async = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e) {
        reject(e);
      }
    };
    var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};

// src/index.ts
var src_exports = {};
__export(src_exports, {
  DEFAULT_MONERYZE_CONFIG: () => DEFAULT_MONERYZE_CONFIG,
  Moneryze: () => Moneryze
});
module.exports = __toCommonJS(src_exports);
var import_xml_js = __toESM(require("xml-js"));

// src/utils/cleanse.ts
var cleanse = (str, spaces = false) => {
  let s = str;
  if (spaces) {
    s = String(s).split(" ").join("");
  }
  return (s ? String(s).split("/").join("").split("=").join("").split("*").join("").split("!").join("").split("-").join("").trim() : "").replace(/\s+/g, " ");
};

// src/utils/format.ts
var snakeToCamelCase = (val) => val.toLowerCase().replace(/[-_][a-z]/g, (group) => group.slice(-1).toUpperCase());
var camelToSnakeCase = (val) => val.replace(/([A-Z])/g, "_$1").toLowerCase();
var pascalToCamelCase = (val) => val.charAt(0).toLowerCase() + val.slice(1);
var removeJsonTextAttribute = (value, parentElement) => {
  try {
    const keyNo = Object.keys(parentElement._parent).length;
    const keyName = Object.keys(parentElement._parent)[keyNo - 1];
    parentElement._parent[keyName] = value;
  } catch (e) {
    console.log(e);
  }
};
var format = (data, sanitize = true) => {
  var _a, _b;
  const response = data;
  if (data.iSO) {
    response.iso = data.iSO;
    delete response.iSO;
  }
  if (sanitize) {
    if ((_a = response.resolveData) == null ? void 0 : _a.maskedPan) {
      response.resolveData.maskedPan = "****************";
    }
  }
  return {
    isSuccess: response.timedOut !== "true" && (response.responseCode === "00" || response.responseCode ? parseInt(response.responseCode, 10) < 50 : false),
    code: response.responseCode,
    message: (response.timedOut === "true" ? "TIMEOUT" : cleanse((_b = response.message) != null ? _b : "")) || "ERROR",
    data: response
  };
};

// src/utils/index.ts
var filter = /* @__PURE__ */ new Set([
  "res_lookup_masked",
  "res_delete",
  "completion",
  "res_update_cc"
]);
var sudo = /* @__PURE__ */ new Set(["res_lookup_masked"]);
var wrapInRequestField = (body) => {
  return {
    request: body
  };
};

// constants/globals.ts
var PROTOCOL = "https";
var PORT = 443;
var HOST = "www3.moneris.com";
var TEST_HOST = "esqa.moneris.com";
var US_HOST = "esplusqa.moneris.com";
var FILE = "/gateway2/servlet/MpgRequest";
var US_FILE = "/gateway_us/servlet/MpgRequest";
var MPI_FILE = "/mpi/servlet/MpiServlet";
var API_VERSION = '"API_VERSION": "PHP NA - 1.2.3"';
var CLIENT_TIMEOUT = 30;

// src/index.ts
var DEFAULT_MONERYZE_CONFIG = {
  appName: "moneryze",
  cryptType: "7",
  isTest: false,
  statusCheck: false
};
var Moneryze = class {
  /**
   * Create a new Moneryze instance.
   * @param config - The configuration for this Moneryze instance.
   */
  constructor(config) {
    this._config = __spreadValues(__spreadValues({}, DEFAULT_MONERYZE_CONFIG), config);
  }
  /**
   * Interfaces with the Moneris host specified in the config.
   * @param type - The type of the transaction to send.
   * @param data - The data / body of the transaction / request.
   * @returns The result of the transaction / request.
   *
   * @example
   * ```ts
   * // Sends a request using the 'res_add_cc' transaction type.
   * moneryze.send("res_add_cc", {
   *   pan: "4242424242424242",
   *   expdate: "2011",
   * });
   * ```
   */
  send(type, data) {
    return __async(this, null, function* () {
      var _a;
      const suffix = `${(/* @__PURE__ */ new Date()).getTime()}-${Math.ceil(
        Math.random() * 1e4
      )}`;
      const out = data;
      if (!filter.has(type)) {
        out.crypt_type = data.cryptType || this._config.cryptType;
        out.order_id = out.order_id || `${cleanse(
          this._config.appName || DEFAULT_MONERYZE_CONFIG.appName,
          true
        )}-Purchase-${suffix}`;
      }
      if (this._config.isTest && out.test) {
        out.amount = 0.05;
        delete out.test;
      }
      if (data.pan) {
        out.pan = cleanse(data.pan, true);
      }
      if (data.expdate) {
        out.expdate = cleanse(data.expdate, true);
      }
      if (out.description) {
        out.dynamic_descriptor = out.description || out.dynamic_descriptor || type;
        delete out.description;
      }
      if (out.token) {
        out.data_key = out.token;
        delete out.token;
      }
      if (out.cvd_info) {
        out.cvd_info = cleanse(out.cvd_info);
      }
      if (out.avs_info) {
        out.avs_info = cleanse(out.avs_info);
      }
      if (type === "kount_inquiry") {
        if (!out.email) {
          out.email = "noemail@kount.com";
        }
        if (!out.auto_number_id) {
          out.auto_number_id = "0123456789";
        }
      }
      const body = {
        storeId: this._config.storeId,
        apiToken: this._config.apiToken,
        statusCheck: (_a = this._config.statusCheck) != null ? _a : false
      };
      if (type === "attribute_query" || type === "session_query") {
        body.risk = {};
        body.risk[type] = out;
      } else {
        body[type] = out;
      }
      let host;
      let path;
      switch (this._config.countryCode) {
        case "US":
          host = US_HOST;
          path = US_FILE;
          break;
        case "CA":
        default:
          host = HOST;
          path = FILE;
          break;
      }
      if (this._config.isTest) {
        host = TEST_HOST;
      }
      if (type === "acs" || type === "txn") {
        path = MPI_FILE;
      }
      const response = yield fetch(`${PROTOCOL}://${host}:${PORT}${path}`, {
        method: "POST",
        body: import_xml_js.default.js2xml(wrapInRequestField(body), {
          compact: true,
          spaces: 4,
          elementNameFn: (val) => camelToSnakeCase(val)
        }),
        headers: {
          "User-Agent": API_VERSION,
          "Content-Type": "text/xml"
        },
        signal: AbortSignal.timeout(CLIENT_TIMEOUT * 1e3)
      });
      const xmlResponse = yield response.text();
      const jsonResponse = import_xml_js.default.xml2js(xmlResponse, {
        compact: true,
        elementNameFn: (val) => val.includes("_") ? snakeToCamelCase(val) : pascalToCamelCase(val),
        textFn: removeJsonTextAttribute
      });
      const receipt = Array.isArray(jsonResponse.response.receipt) ? jsonResponse.response.receipt[0] : jsonResponse.response.receipt;
      return format(receipt, !sudo.has(type));
    });
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  DEFAULT_MONERYZE_CONFIG,
  Moneryze
});
//# sourceMappingURL=index.js.map